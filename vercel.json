{
  "buildCommand": "npm run build -- my-client --configuration=production && cp -r dist/my-client/browser/* dist/my-client/ && rm -rf dist/my-client/browser",
  "outputDirectory": "dist/my-client",
  "framework": null,
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
